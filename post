#!/usr/bin/env bash

set -euo pipefail

title="${*:-}"
slug=$(echo "$title" | tr '[:upper:]' '[:lower:]' | sed -E 's/[^a-z0-9]+/-/g; s/^-+|-+$//g')

hugo new content "content/blog/${slug}.md" --editor "$EDITOR"
